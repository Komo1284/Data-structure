// 05. review.c
#include <stdio.h>

int main(void) {
	// Stack - 벽돌을 옮기는 것과 비슷함
	// 1) 가장 기초가 되는 자료구조 -> 이를 바꿔쓰면서 시작됨
	// 2) 보관되었다가 이를 "꺼낸다" -> 필요한 곳에 복사 후 원본 제거
	// 3) 일반적인 상식의 순서와 다른 역순이 기본
	//   -> 정순으로 볼려면, 재귀함수가 끼어듬
	return 0;
}